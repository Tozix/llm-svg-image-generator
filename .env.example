# Порт HTTP-сервера. По умолчанию 3000.
# PORT=3000

# JWT и вход в API / веб-интерфейс
JWT_SECRET=your-secret-change-in-production
API_USER=admin
API_PASSWORD=admin

# API (обязательные для генерации)
API_ENDPOINT=https://qwen.agent-lia.ru/api/v1/chat/completions
API_KEY=Bearer YOUR_API_KEY
MODEL=qwen-max-latest

# Стриминг ответа API (SSE). По умолчанию false.
# STREAM_ENABLED=true

# Расширение описания через дополнительный запрос к LLM. По умолчанию false.
# При таймаутах лучше выключить: описание не расширяется, промпт короче, ответ быстрее.
# ENABLE_EXTENDED_DESCRIPTION=true

# Подробное логирование (debug). Варианты: LOG_LEVEL=debug или DEBUG=1
# LOG_LEVEL=debug
# DEBUG=1

# Сколько раз повторять запрос при неполном/невалидном SVG. По умолчанию 3.
# MAX_VALIDATION_RETRIES=3

# Лимит токенов на ответ при генерации SVG (меньше — быстрее, но SVG может обрезаться). По умолчанию 8000.
# MAX_GENERATION_TOKENS=8000

# Максимум элементов в SVG (path, rect, circle…). Меньше — быстрее генерация. По умолчанию 80.
# MAX_SVG_ELEMENTS=80

# Размеры холста (пиксель-арт: меньшие значения — проще для LLM, узнаваемые объекты).
# SCENE_WIDTH=640   — ширина сцены (режим scene). По умолчанию 640. Диапазон 256–2048.
# SCENE_HEIGHT=480  — высота сцены. По умолчанию 480. Диапазон 256–2048.
# OBJECT_SIZE=256   — сторона квадрата для режимов object, character, map. По умолчанию 256. Диапазон 64–1024.

# Worker: максимум одновременных запросов генерации (очередь). По умолчанию 3.
# MAX_CONCURRENT_JOBS=3

# Композитная генерация: разбить сцену на элементы (до 20), генерировать фрагменты параллельно, собрать в один SVG.
# COMPOSITE_SCENE=true
# GRID_COLS=16
# GRID_ROWS=12
# COMPOSITE_CONCURRENCY=5
